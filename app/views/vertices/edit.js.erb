var lat = <%= @vertex.latitude %>
var lng = <%= @vertex.longitude %>
var id = <%= @vertex.id %>
$("#vertices").find('.vertex').each(function(){
  if($(this).data('vid') == id){
    $(this).data('latitude',lat);
    $(this).data('longitude',lng);
  }
});

